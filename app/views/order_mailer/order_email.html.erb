<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thanks for shopping with us!</h1>
    <table class="table table-bordered">
    <thead>
      <tr>
        <th colspan="2">Product</th>
        <th>Quantity</th>
        <th>Total Item Price</th>
      </tr>
    </thead>
    <tbody>
      <% @order_total = 0 %>
      <% @order.line_items.product.each do |item, quantity| %>
        <%= render 'items', item: item, quantity: item.quantity %>
      <% end %>
    </tbody>
      <tfoot>
        <tr>
          <td colspan="3">
            TOTAL:
          </td>
          <td>
            <%= @order_total %>
          </td>
        </tr>
      </tfoot>
  </table>
  </body>
</html>